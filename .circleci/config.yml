version: 2
jobs:
  build:
    docker:
      - image: docker:stable-git
    steps:
      - checkout
      - run: git submodule update --init --recursive
      - setup_remote_docker:
          version: 17.05.0-ce
      # build the image
      - run: docker build -t ampersand8/credhub .
